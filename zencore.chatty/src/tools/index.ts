export { Tools, ErrorHandler, DateTimeUtils, StringUtils, ValidationUtils, FileUtils } from './utils';

/**
 * 工具类集合 - 向后兼容的导出
 */
// export const Tool = {
//     bear(token: string) {
//         return `Bearer ${token}`;
//     },
// };  